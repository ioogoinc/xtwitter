require 'rails_helper'

RSpec.describe "Api::Authentications", type: :request do
  describe "GET /index" do
    # user = create(:user)
    # token = Api::JsonWebToken.encode({sub: 1})
    # auth_headers = token
    # request_headers = { 'Authorization' => "Bearer #{auth_headers['access-token']}" }
    # # Action
    # get "/api/tweets", headers: request_headers

    # # Assertion
    # puts response.body
  end
end
